export function ASIGNARG_LATITUD(state, datos) {
    state.lat = datos
}

export function ASIGNARG_LONGITUD(state, datos) {
    state.lon = datos
}

export function ASIGNAR_PET_ID(state, datos) {
    state.pet_id = datos
}

export function ASIGNAR_COMMENT(state, datos) {
    state.comment = datos
}
